# r2-notify-demo
R2-Notify Demo is a lightweight demo application for real-time notifications that uses r2-notify react library, r2-notify-client library and the r2-notify-server as the backend notification hub
